#include <stdio.h>

int main() {
    int maxEven;
    
    scanf("%d", &maxEven);
    
    if ( maxEven % 2 != 0 ) {
        maxEven -= 1;
    }
    printf("%d\n", maxEven);
    
    return 0;
}


/*

Условие задачи

Считать целое число. Вывести на экран максимальное четное число, не большее введенного.

Пример ввода

17

Пример вывода

16

*/
